// C refreshers

/* Operators */
// << - left shift
// >> - right shift

/* VM-related notes */
// Instruction set - PUSH, POP, ADD, SUB, MUL, DIV, AND, OR, XOR, NOT
// Store all instructions by converting to bytecode


#include <stdio.h>
#include <stdint.h>
#include <stdlib.h>
#include <stdbool.h>

void load() { // load data from disk into memory
    printf("Loading data...\n");
}





int main() { // main loop
    return 0;
}